backend:
  name: git-gateway
  branch: main # Branch to update

media_folder: "public/assets/images" # Media files will be stored in the repo under public/assets/images
public_folder: "/assets/images" # The src attribute for uploaded media will begin with /assets/images

collections:
  - name: "products"
    label: "Products"
    folder: "src/data" # We will use a file collection instead of a folder collection for a single JSON array, but Decap supports file collections.
    # Wait, if we use a file collection, we must define 'files'
    # Let me redo this file content correctly.
